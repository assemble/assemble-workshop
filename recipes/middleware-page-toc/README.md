# Middleware to bind toc to view

This recipe uses a middleware and markdown-toc to bind a table of contents to the view and then uses this data in the layout to render a specific table of contents.